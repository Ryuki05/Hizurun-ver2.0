/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'via.placeholder.com'], // localhostとplaceholderのドメインを追加
    },
};

export default nextConfig;
